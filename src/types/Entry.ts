type Entry = {
    id:number,
    userId:number,
    startDate: string,
    endDate: string,
    description: string,
    technologies: Array<string>
    role: string,
    linkToRepos: string
}

export default Entry;